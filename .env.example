# JWT Secret - REQUIRED FOR PRODUCTION!
# CRITICAL: Application will not start without this variable set
# Generate a strong random secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# NEVER use the default value in production!
JWT_SECRET=your-secret-key-change-this-in-production-use-at-least-64-characters

# Server Configuration
PORT=3000
NODE_ENV=development

# Admin Token (for backward compatibility, not recommended)
# Use the admin role system instead via scripts/make_admin.js
ADMIN_TOKEN=your-admin-token-change-this

# Database Path (optional, defaults to ./database/comptia.db)
# DATABASE_PATH=./database/comptia.db

# Data Retention (in days)
# AUTH_LOGS_RETENTION=90
# AUDIT_LOGS_RETENTION=180
# DELETED_USERS_RETENTION=30
# DELETED_ATTEMPTS_RETENTION=30

# Rate Limiting
# AUTH_RATE_LIMIT_WINDOW_MS=900000
# AUTH_RATE_LIMIT_MAX=10
# API_RATE_LIMIT_WINDOW_MS=900000
# API_RATE_LIMIT_MAX=100
